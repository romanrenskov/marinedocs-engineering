# Src

Карта смысловой архитектуры сайта.

Текущий MVP - одностраничный React/Vite-лендинг. Основная структура сейчас в `src/App.jsx`, данные в `src/data/siteContent.js`, стили в `src/styles.css`. В будущем можно разнести на компоненты, секции, контент, стили и lib-модули.

